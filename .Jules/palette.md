## 2024-08-06 - Keyboard accessibility (focus states)
**Learning:** The game had interactive elements (buttons, icons) but lacked `:focus-visible` styles, making keyboard navigation difficult.
**Action:** Added distinct focus rings using existing design system colors (`#7deaff`) to `.btn` and `.icon` classes.
